input#id(type='hidden', name='id', value=question.id)

.form-group
    label(for='text') Text
    textarea#text.form-control(name='text') #{question.text}

.form-group
    input.form-check-input(id="isMulti" name="isMulti" value="true" type="checkbox" checked=question.multi)
    label.form-check-label(for="isMulti") Has more than one answer?

button.btn.btn-success(type='submit') Save